{
  "_comment": "WO-122 injection fixture: deploymentRequestId contains a path traversal sequence (..  double-dot). Should be rejected with SHELL_UNSAFE_INPUT before any service dispatch.",
  "deploymentRequestId": "../../etc/passwd",
  "pipelineId": "pipeline-safe-003",
  "stepId": "step-safe-003",
  "fallbackTaskId": "fallback-safe-003"
}
